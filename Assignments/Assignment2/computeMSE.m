%Helper function used to calculate the mean square root error
function mse = computeMSE(groundLabels, estLabels)

    %TODO: Actually write this out if you have time
    mse = immse(groundLabels, estLabels);

end

